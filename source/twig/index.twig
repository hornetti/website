{% extends 'default.twig' %}

{% block page %}

    <div><b>github\horn></b> bio init</div>
    <h1>{{ myName }}</h1>
    <h2>{{ myDescription }}</h2>
    <h3>{{ myBirth }}</h3>
    <h4>{{ myBio }}</h4>
    <h4>{{ myMail }}</h4>

{% endblock %}